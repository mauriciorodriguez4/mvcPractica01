@model IEnumerable<mvcPractica01.Models.estados_reserva>

@{
    ViewData["Title"] = "Estado Reserva";
}

<h1>Estados de reservas</h1>

<p>
    <a class="btn btn-primary" asp-action="Create">Nuevo registro</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.estado)
            </td>
            <td>
                    <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.estado_res_id">Editar</a> |
                    <a class="btn btn-secondary text-light" asp-action="Details" asp-route-id="@item.estado_res_id">Detalles</a> |
                    <a class="btn btn-danger text-light" asp-action="Delete" asp-route-id="@item.estado_res_id">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
